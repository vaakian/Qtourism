const packageControllers = require('./package')
const userControllers = require('./user')
const orderControllers = require('./order')
const merchantControllers = require('./merchant')
module.exports = {
  packageControllers,
  userControllers,
  orderControllers,
  merchantControllers
}